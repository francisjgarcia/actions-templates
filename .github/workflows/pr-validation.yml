---
name: PR validation

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - edited
      - synchronize
      - reopened

jobs:
  pr-validation:
    name: Validate PR title
    uses: ./.github/workflows/wf-pr-validation.yml
